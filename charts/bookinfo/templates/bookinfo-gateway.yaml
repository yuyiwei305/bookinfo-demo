apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: {{.Values.appname}}-gateway
spec:
  selector:
    istio: alb-ingressgateway  # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
